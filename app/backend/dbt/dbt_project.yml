name: app
version: "0.1.0"
config-version: 2

profile: app

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  app:
    +materialized: view
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: core
      +materialized: table
