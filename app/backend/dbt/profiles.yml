dmp:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'postgres') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | int }}"
      user: "{{ env_var('POSTGRES_USER', 'dmp') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'dmp') }}"
      dbname: "{{ env_var('POSTGRES_DB', 'dmp') }}"
      schema: analytics
      threads: 4
